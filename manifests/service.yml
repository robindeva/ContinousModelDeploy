apiVersion: v1
kind: Service
metadata:
    name: robindevacontinousmodeldeploy
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: robindevacontinousmodeldeploy